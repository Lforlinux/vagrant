Vagrant.configure("2") do |config|
  config.vm.box = "williamyeh/centos7-docker"
  config.vm.define "web" do |web|
  config.vm.network "public_network"
  end


#config.vm.provider :virtualbox do |vb|
#        vb.name = "barhost"
end


Vagrant.configure("2") do |config|
  config.vm.box = "geerlingguy/centos7"
  config.vm.define "cent7" do |cent7|
  config.vm.network "public_network"
  end
end


Vagrant.configure("2") do |config|
  config.vm.box = "lforlinux/centos7"
  config.vm.box_version = "1.1.0"
  config.vm.network "public_network"
  config.vm.define "kln" do |kln|
  end
end


Vagrant.configure("2") do |config|
  config.vm.box = "lforlinux/centos7"
  config.vm.box_version = "1.1.0"
  config.vm.network "public_network"
  config.vm.define "adp" do |adp|
  end
end


Vagrant.configure("2") do |config|
  config.vm.box = "lforlinux/centos7"
  config.vm.box_version = "1.1.0"
  config.vm.network "public_network"
  config.vm.define "adp" do |adp|
  end
end

Vagrant.configure("2") do |config|
  config.vm.box = "lekshmin/centos7-docker"
  config.vm.box_version = "1.1.1"
  config.vm.network "public_network"
  config.vm.define "docker" do |docker|
  end
end











